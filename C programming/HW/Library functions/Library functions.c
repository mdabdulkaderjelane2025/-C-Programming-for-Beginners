#include<assert.h>
//It checks the value of an expression that we expect to be true under normal circumstances.
//If the expression is a nonzero value, the assert macro does nothing.
#include<complex.h>
// A set of functions for manipulating complex numbers.
#include<float.h>
//Defines macro constants specifying the implementation-specific properties of the floating-point library.
#include<limits.h>
//	These limits specify that a variable cannot store any value beyond these limits, for example-An unsigned character can store up to a maximum value of 255.
#include<math.h>
//The math.h header defines various mathematical functions and one macro. All the Functions in this library take double as an argument and return double as the result.
#include<stdio.h>
//The stdio.h header defines three variable types, several macros, and various function for performing input and output.
#include<time.h>
//Defines date and time handling functions.
#include<string.h>
//Strings are defined as an array of characters. The difference between a character array and a string is that a string is terminated with a special character ‘\0’

int main()
{
    return 0;
}
